---
model: Claude Sonnet 4.5
description: 'Create an agent in the file `./agents/terraform-test.md` that simulates the behavior of the Terraform MCP server when executing a `terraform apply` command. The agent should follow these rules:
---
Using the terraform mcp server write and agent in the ./agets/terraform-test.md with the rules on how to simulate the terraform apply.